{
  "name": "geoestate-api",
  "version": "2.1.0",
  "description": "GeoEstate API — OTP, Registration, Properties, Owner Portal (Supabase)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.11.0"
  }
}
